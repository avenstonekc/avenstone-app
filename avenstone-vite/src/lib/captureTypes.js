// Capture mode — drives AR config branching (Phase 2+) and data tagging
export const CaptureMode = { INTERIOR: 'interior', EXTERIOR: 'exterior' };

// Height source — how the height value was obtained
export const HeightSource = { AUTO: 'auto', MANUAL: 'manual', ESTIMATED: 'estimated' };
