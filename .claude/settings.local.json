{
  "permissions": {
    "allow": [
      "mcp__Claude_Preview__preview_start",
      "Bash(npm install:*)",
      "Bash(start \"\" docs/test.md)",
      "Bash(git add:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path',''); exit(0 if 'avenstone-vite' in f and 'src' in f and f.endswith(('.jsx','.js','.tsx','.ts')) else 1)\" && npx playwright test tests/portals-e2e.spec.js --reporter=list 2>&1 || true",
            "timeout": 1800,
            "statusMessage": "Running Playwright tests..."
          }
        ]
      }
    ]
  }
}
